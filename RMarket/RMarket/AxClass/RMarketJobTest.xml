<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>RMarketJobTest</Name>
	<SourceCode>
		<Declaration><![CDATA[
internal final class RMarketJobTest
{
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>main</Name>
				<Source><![CDATA[
    /// <summary>
    /// Class entry point. The system will call this method when a designated menu 
    /// is selected or when execution starts and this class is set as the startup class.
    /// </summary>
    /// <param name = "_args">The specified arguments.</param>
    public static void main(Args _args)
    {
        /*RMarketAPIEng_Time apiClient;
        int64 seconds;
        UtcDateTime gmtTime;

        try
        {
            apiClient = RMarketAPIEng::construct("Binance", RMarketAPIMethodEnum::GetServerTime);
            apiClient.init();
            seconds = apiClient.fetchData();
        }
        catch
        {
            throw error("");
        }

        gmtTime = DateTimeUtil::newDateTime(01\01\1970, 0, Timezone::GMT_COORDINATEDUNIVERSALTIME);
        gmtTime = DateTimeUtil::addSeconds(gmtTime, seconds);
        Info(strFmt("%1", gmtTime));*/
        Info(RMarketCandleView::NextTimestamp());
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>